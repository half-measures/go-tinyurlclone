2. both BASE_URL and MARIADB_URI are hardcoded in main.go and have local defaults.

Docker demands more config and study for a perfect bit. Must remember to be careful on the titles and the newer Vers with context and build args.
Would help to draw more too. The bigger bit on this was Depploying SSL, that was super easy, but
Denotes test requirements from the ground up, need a .env from the start also.

##TODO
